{
  "name": "atrovia-co-site",
  "version": "1.0.0",
  "private": true,
  "description": "Atrovia marketing site — static HTML + Stripe/Resend API (runs on Render via Express, or Vercel serverless)",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "deploy": "powershell -ExecutionPolicy Bypass -File ./scripts/setup-and-deploy.ps1"
  },
  "dependencies": {
    "express": "^4.22.2",
    "resend": "^3.5.0",
    "stripe": "^16.12.0"
  }
}
